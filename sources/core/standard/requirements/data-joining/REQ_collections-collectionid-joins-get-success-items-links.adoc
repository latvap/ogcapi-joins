[[req_data_joining_collections-collectionid-joins-get-success-items-links]]

[requirement]
====
[%metadata]
identifier:: /req/data-joining/collections-collectionid-joins-get-success-items-links
part:: For each item included in the `joins` array in the response, the `links` property of that item SHALL include a link for each supported encoding to the join source resource (relation: `join`).
part:: All links SHALL include the `rel` and `type` properties.
====